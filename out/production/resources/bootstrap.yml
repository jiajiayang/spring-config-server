spring:
  application:
    name: spring-config-server
  cloud:
    config:
      server:
        git:
          uri: /Users/lofy/Documents/code/spring-conf-repo

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8001/eureka,http://127.0.0.1:8002/eureka,http://127.0.0.1:8003/eureka
  instance:
    prefer-ip-address: true
management:
  health:
    refresh:
      enabled: true